PLAYERS
    PLAYER_ID           INT
    NAME                VARCHAR(32)
    CODE                VARCHAR(10)
    GENDER              SMALLINT

VENUES
    VENUE_ID            INT
    NAME                VARCHAR(256)

TEAMS
    TEAM_ID             INT
    NAME                VARCHAR(256)
    GENDER              SMALLINT
    TEAM_TYPE           SMALLINT -- (Club or international)

TOURNAMENTS
    TOURNAMENT_ID       INT
    TOURNAMENT_NAME     VARCHAR(256)
    SEASON              VARCHAR(32)

TOURNAMENT_TEAMS
    TOURNAMENT_ID       INT
    TEAM_ID             INT

MATCH_PLAYERS
    MATCH_ID            INT
    TEAM_ID             INT
    PLAYER_ID           INT

MATCHES
    MATCH_ID            INT
    TOURNAMENT_ID       INT
    DATE                DATE
    NAME                VARCHAR(256)
    VENUE_ID            INT
    HOME_TEAM_ID        INT
    AWAY_TEAM_ID        INT
    GENDER              SMALLINT
    MATCH_FORMAT        VARCHAR(32) -- (Test, ODI, T20, etc..)
    MATCH_TYPE          VARCHAR(32) -- (Men's, Women's, etc..)
    BALLS_PER_OVER      INT
    OVERS_PER_INNINGS   INT
    INNINGS_PER_MATCH   INT
    RESULT              VARCHAR(16) -- (Win, draw, tie, no-result, abandoned, etc)
    RESULT_TYPE         VARCHAR(16) -- (Normal, DLS method, etc..)
    WINNER              INT -- (winning team id)
    WIN_TYPE            VARCHAR(16) -- (Runs, wickets)
    WIN_BY              INT -- (No. of runs/wickets)
    PLAYER_OF_THE_MATCH INT -- (player_id)
    TOSS_WINNER         INT -- (team_id)
    TOSS_DECISION       VARCHAR(16) -- (Bat, field)

DELIVERIES
    MATCH_ID            INT
    BOWLER_ID           INT
    STRIKER_ID          INT
    NON_STRIKER_ID      INT
    BATTING_TEAM_ID     INT
    BOWLING_TEAM_ID     INT
    INNINGS             SMALLINT
    OVER                INT
    BALL                INT
    RUNS_BAT            INT
    RUNS_EXTRAS         INT
    OUTCOME_TYPE        VARCHAR(32) -- (Wicket, dot, runs, extras, etc)
    OUTCOME__KIND       VARCHAR(32) -- (Caught, runout, LBW, Wide, Noball,etc....)
    OUTCOME_PLAYER_ID   VARCHAR(32) -- (Caught by player, runout player)