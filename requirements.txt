# Runtime dependencies
psycopg2-binary   # PostgreSQL adapter
geopy             # Venue geocoding for populate_venue_countries.py

# API
fastapi
uvicorn[standard]
pydantic>=2.0
PyJWT[crypto]     # JWT verification for Supabase auth — [crypto] adds cryptography for ES256/RS256
python-dotenv     # loads .env at startup; no-op in production where env vars are set by the host
